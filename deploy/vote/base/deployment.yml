apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vote
    tier: vote
  name: vote
  namespace: instavote
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
      tier: vote
  strategy: {}
  template:
    metadata:
      labels:
        app: vote
        tier: vote
    spec:
      containers:
      - image: schoolofdevops/vote:v4
        name: vote
        resources: {}
